import React from "react";

const Loading = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <button className="btn bg-primary text-accent border-none loading">
        Loading
      </button>
    </div>
  );
};

export default Loading;
